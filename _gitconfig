# Git config
# 2013/04/17 by Monpig

[user]
    name = Monmon
    email = monmonpig@gmail.com
#	name = Monmon
#	email = monmon@dotdev.com.au
[color]
    ui = true

[core]
    editor = vim

[merge]
    tool = vim

[alias]
    st = status
    ct = commit -m
    cta = commit -am
    co = checkout
    br = branch
    df = diff
    addu = add -u :/
    addthis = add -u .
    pushom = push origin master
    reb = rebase -i
    rebc = rebase --continue
    reba = rebase --abort
    logno = log --name-only

    mm = merge master

    com = checkout master

[push]
    default = simple
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
