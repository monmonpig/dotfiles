# Git config
# 2013/04/17 by Monpig

[user]
    name = Monmon
    email = monmonpig@gmail.com
[color]
    ui = true

[core]
    editor = vim

[merge]
    tool = vim

[alias]
    st = status
    ct = commit -m
    cta = commit -am
    co = checkout
    br = branch
    df = diff
    addu = add -u :/
    addthis = add -u .
    pushom = push origin master
    reb = rebase -i
    rebc = rebase --continue
    reba = rebase --abort
    logno = log --name-only

    mm = merge master

    com = checkout master

[push]
    default = simple
